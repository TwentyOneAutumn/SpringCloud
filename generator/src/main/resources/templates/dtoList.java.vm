package ${package.Entity}.dto;

import lombok.Data;
import lombok.NoArgsConstructor;
import lombok.AllArgsConstructor;
import com.core.doMain.PageEntity;
import javax.validation.constraints.NotBlank;
import javax.validation.constraints.NotEmpty;
import javax.validation.constraints.NotNull;
import com.fasterxml.jackson.annotation.JsonFormat;
import java.util.Date;
import lombok.EqualsAndHashCode;
import com.core.doMain.TimeEntity;

/**
 * ${table.entityName} 列表接口参数封装类
 */
@Data
@NoArgsConstructor
@AllArgsConstructor
@EqualsAndHashCode(callSuper = true)
public class ${table.entityName}ListDto extends PageEntity{

#foreach($field in ${table.fields})
#if(${field.keyFlag})
    /**
    * ${field.comment}
    */
    #if(${field.propertyType} == 'String')
    @NotBlank(message = "${field.propertyName}参数不能为空")
    #elseif(${field.propertyType} == 'Integer')
    @NotNull(message = "${field.propertyName}参数不能为空")
    #end
    private ${field.propertyType} ${field.propertyName};
#else
    #if(${field.name} != 'is_deleted' && ${field.name} != 'create_time' && ${field.name} != 'update_time')
    /**
    * ${field.comment}
    */
    #if(${field.propertyType} == 'LocalDateTime' || ${field.propertyType} == 'LocalDate')
    @NotNull(message = "${field.propertyName}参数不能为空")
    @JsonFormat(pattern = "yyyy-MM-dd",timezone = "GMT+8")
    private Date ${field.propertyName};
    #else
    #if(${field.propertyType} == 'String')
    @NotBlank(message = "${field.propertyName}参数不能为空")
    #else
    @NotNull(message = "${field.propertyName}参数不能为空")
    #end
    private ${field.propertyType} ${field.propertyName};
    #end
    #end
#end
#end
}