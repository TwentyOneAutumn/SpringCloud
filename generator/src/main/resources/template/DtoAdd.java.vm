package ${package}.${moduleName}.domain.dto;

import lombok.Data;
import javax.validation.constraints.NotBlank;
import javax.validation.constraints.NotEmpty;
import javax.validation.constraints.NotNull;
import java.util.Date;
import java.io.Serializable;
import com.fasterxml.jackson.annotation.JsonFormat;

/**
 * ${className}Dto
 */
@Data
public class ${className}AddDto implements Serializable {
#foreach ($column in $columns)
#if( $column.attrname != 'isDeleted' && $column.attrname != 'createTime' && $column.attrname != 'updateTime' && $column.attrname != ${pk.attrname})
#if( ($column.attrType == 'LocalDateTime' || $column.attrType == 'LocalDate'))
    /**
     * $column.comments
     */
    @NotNull(messageInfo = "$column.attrname参数不能为空")
    @JsonFormat(pattern = "yyyy-MM-dd",timezone = "GMT+8")
    private Date $column.attrname;
#else
#if($column.attrType == 'String')
    /**
     * $column.comments
     */
    @NotBlank(messageInfo = "$column.attrname参数不能为空")
    private $column.attrType $column.attrname;
#elseif($column.attrType == 'Boolean')
    /**
     * $column.comments
     */
    @NotNull(messageInfo = "$column.attrname参数不能为空")
    private $column.attrType $column.attrname;
#end
#end
#end
#end
}
