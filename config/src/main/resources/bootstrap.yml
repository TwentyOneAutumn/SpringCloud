# IP
ip: 124.221.27.253

spring:
  main:
    banner-mode: off
    allow-bean-definition-overriding: true
  cloud:
    nacos:
      discovery:
        server-addr: ${ip}:8848
        username: nacos
        password: nacos
  redis:
    host: ${ip}
    port: 6379
    password: root

# ClientDetailsService
client-details:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    jdbcUr: jdbc:mysql://${ip}:3306/client?serverTimezone=UTC&useUnicode=true&characterEncoding=UTF-8&useSSL=false&allowPublicKeyRetrieval=true
    username: root
    password: 2762581@com